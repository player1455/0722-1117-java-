import java.util.*;


public class Solution {
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 
     * @param n int整型 
     * @return string字符串ArrayList
     */
    //Set<string> set = new HashSet<>();
    public ArrayList<String> generateParenthesis (int n) {//返回int值
        // write code here
        int length = n*2;
        StringBuffer sb = new StringBuffer();
        for(int i = 0;i < n;i++){
            
        }
    }
}